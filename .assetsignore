# Arquivos que NÃO devem ser publicados como estáticos (Cloudflare Workers Assets).
# assets.directory é "." (raiz), então tudo que não é do frontend precisa ficar fora.
.git
.wrangler
worker
server
docs
node_modules
wrangler.jsonc
package.json
package-lock.json
README.md
CLAUDE.md
.assetsignore
.gitignore
