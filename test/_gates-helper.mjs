// O nível de ENTRADA anunciado, em número de EXIBIÇÃO (o que o editor lê).
// Mora fora do teste pra que mudá-lo seja um ato deliberado e visível no diff,
// não um número reescrito no meio de uma asserção.
export const MIN_RANK_WAZE_DISPLAY = 3;
